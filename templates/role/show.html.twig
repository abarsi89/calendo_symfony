{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>RoleId</th>
                <td>{{ role.roleId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_role_index') }}">back to list</a>

    <a href="{{ path('app_role_edit', {'roleId': role.roleId}) }}">edit</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
