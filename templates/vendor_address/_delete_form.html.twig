<form method="post" action="{{ path('app_vendor_address_delete', {'vendorAddressId': vendor_address.vendorAddressId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vendor_address.vendorAddressId) }}">
    <button class="btn">Delete</button>
</form>
